# Week 0 — Raw API

See raw `tool_calls` JSON before any framework.

```bash
npm run w0:groq
npm run w0:ollama
```

If Groq 429s during repeated runs, edit `src/week-00-raw-api/groq-raw.ts` — use `llama-3.1-8b-instant`.

Run from **repo root** (`agent-lab/`).
